{
  "name": "vees-click-to-app",
  "version": "0.1.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "package-win": "electron-packager . --overwrite --asar --platform=win32 --arch=all --prune=true --out=release-builds",
    "package-linux": "electron-packager . license-scanning-app --overwrite --asar --platform=linux --prune=true --out=release-builds"
  },
  "author": "",
  "devDependencies": {
		"electron": "10.1.3"
	},
  "license": "ISC"
}
